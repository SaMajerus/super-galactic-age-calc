import { ageCalc } from '../src/js/ageCalc.js';

describe('ageinDays', () => {
  
  test("Should return user's correct age in days, based on entered birthdate", () => {
    
    expect(bdDays).toEqual();
  });
});